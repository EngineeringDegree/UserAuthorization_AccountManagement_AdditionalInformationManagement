/**
 * Logo object to display
 * @param {object} props 
 * @returns jsx of the logo wrapper
 */
const Logo = (props) => {
    return (
        <div>
            <img src="/img/logo.png" className="logo" alt="logo" />
        </div>
    )
}

export default Logo