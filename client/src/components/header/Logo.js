/**
 * Logo object to display
 * @param {object} props 
 * @returns jsx of the logo wrapper
 */
const Logo = (props) => {
    return (
        <div>
            Logo
        </div>
    )
}

export default Logo