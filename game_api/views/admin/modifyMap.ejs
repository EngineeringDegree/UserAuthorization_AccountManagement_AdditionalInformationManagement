<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/tags/metaTags') %>
        <%- include('../partials/tags/favicon') %>
            <title>Ninergames</title>
            <%- include('../partials/links/mainLinks') %>
</head>

<body>
    <%- include('../partials/noscript/noscript') %>
        <%- include('../partials/components/menu') %>
            <%- include('../partials/components/breadcrumb') %>

                <input id="map-id" type="hidden" value="<%= map._id %>" />
                Map Name: <input id="map-name" type="text" value="<%= map.name %>" />
                Map Size in formats (number(width and heght)) or (number(width)xnumber(height): <input id="map-size"
                    type="text" value="<%= map.size %>" />
                Map Image: <input id="map-image" type="text" value="<%= map.image %>" />
                Map Fields: <input id="map-fields" type="text" value="<%= map.fields[0] %>" />
                Map Starting Positions: <input id="map-starting" type="text" value="<%= map.startingPositions[0] %>" />
                Map Description: <input id="map-description" type="text" value="<%= map.description %>" />
                <% if(map.readyToUse) { %>
                    Ready to use: <input id="map-ready" type="checkbox" checked />
                    <% } else { %>
                        Ready to use: <input id="map-ready" type="checkbox" />
                        <% } %>
                            <div>
                                <img src="#" id="image-display" class="map display" alt="Map not found" />
                                <div id="field-grid" class="cube-overlay"></div>
                            </div>
                            <%- include('../partials/components/footer') %>

                                <%- include('../partials/links/authorizationServer') %>
                                    <%- include('../partials/links/authorizeAdmin') %>
                                        <%- include('../partials/links/modifyMap') %>
</body>

</html>